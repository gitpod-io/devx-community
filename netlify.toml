[build]
  command = "npm run build"
  publish = "build/"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "src/functions"
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"
  targetPort = 5173

